# External Images

Images that are not generated by plotting code.

Binary files here are **gitignored by default** (see the sil-managed block in `.gitignore`).
Document each image below so provenance is still recoverable.

## List of images
<!-- For each image: filename, origin, license, figure reference in the paper. -->
