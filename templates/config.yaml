project:
  title: ""
  stage: draft                 # draft | prep | review | final

paths:
  sources: ./sources
  data: ./data
  figures: ./figures
  agent: ./agent

latex:
  engine: tectonic             # tectonic | latexmk | pdflatex | xelatex | lualatex
  main: paper_draft.tex
  template: standard           # standard | neurips | icml | iclr | ieee | arxiv

parsing:
  engine: marker
