# sources/

Store literature PDFs here for parsing with `sil parse`.

> **Note:** Binary files (PDFs, images, etc.) in `sources/` are gitignored by default
> to keep the repository lightweight. Original PDFs are indexed into SQLite upon running `sil parse`.
