# Generated Plots

Plots produced by code (matplotlib, etc.).

Plot binaries are **gitignored by default** (see the sil-managed block in `.gitignore`).
Document each plot below (script + paper figure ref) so figures stay reproducible.

## List of plots
<!-- For each plot: filename, script that generated it, figure reference in the paper. -->
