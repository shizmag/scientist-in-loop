# Deprecated as the install path.
#
# Use uv from the repository root instead (see root pyproject.toml):
#   uv sync                 # pypdf
#   uv sync --group dev     # + golden_dataset (pyyaml, jsonschema)
#   uv sync --extra marker  # + marker-pdf
#
# Optional lock export (not required for normal use):
#   uv export --no-dev --no-emit-project -o python/requirements.export.txt
